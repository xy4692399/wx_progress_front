<view class="container">
  <view class="card">
    <view class="card-title">新增支出</view>
    <view class="form-group">
      <view class="form-item">
        <text class="form-label">日期</text>
        <picker mode="date" :value="formData.recordDate" bindchange="onDateChange">
          <view class="picker-value">{{formData.recordDate || '选择日期'}}</view>
        </picker>
      </view>
      <view class="form-item">
        <text class="form-label">分类</text>
        <picker :value="categoryIndex" :range="categories" bindchange="onCategoryChange">
          <view class="picker-value">{{categories[categoryIndex] || '选择分类'}}</view>
        </picker>
      </view>
      <view class="form-item">
        <text class="form-label">金额</text>
        <input class="input-field" type="digit" placeholder="请输入金额" value="{{formData.amount}}" bindinput="onAmountInput" />
      </view>
      <view class="form-item">
        <text class="form-label">票据照片</text>
        <view class="upload-area" bindtap="uploadImage">
          <image wx:if="{{formData.ticketUrl}}" :src="formData.ticketUrl" class="upload-image" />
          <view wx:else class="upload-placeholder">
            <text class="upload-icon">+</text>
            <text class="upload-text">上传票据</text>
          </view>
        </view>
      </view>
      <view class="form-item">
        <text class="form-label">备注</text>
        <textarea class="input-field" placeholder="选填" value="{{formData.remark}}" bindinput="onRemarkInput" />
      </view>
      <button class="btn-primary" bindtap="submitForm">保存</button>
    </view>
  </view>

  <view class="card">
    <view class="card-header">
      <text class="card-title">支出记录</text>
      <view class="filter-bar">
        <picker mode="date" :value="filterDate" bindchange="onFilterDateChange">
          <view class="filter-btn">{{filterDate || '筛选日期'}}</view>
        </picker>
      </view>
    </view>
    <view class="record-list">
      <view class="record-item" wx:for="{{records}}" wx:key="id" wx:index="index" data-index="{{index}}" bindtap="showDetail">
        <view class="record-info">
          <view class="record-category">{{getCategoryName(item.category)}}</view>
          <view class="record-date">{{item.recordDate}}</view>
        </view>
        <view class="record-amount">-{{formatMoney(item.amount)}}</view>
      </view>
    </view>
    <view wx:if="{{records.length === 0}}" class="empty-state">
      <text class="empty-text">暂无支出记录</text>
    </view>
  </view>
</view>

<view class="modal-mask" wx:if="{{showModal}}" bindtap="closeModal">
  <view class="modal-content" catchtap="preventClose">
    <view class="modal-title">支出详情</view>
    <view class="modal-body">
      <view class="detail-item">
        <text class="detail-label">日期</text>
        <text class="detail-value">{{selectedRecord.recordDate}}</text>
      </view>
      <view class="detail-item">
        <text class="detail-label">分类</text>
        <text class="detail-value">{{getCategoryName(selectedRecord.category)}}</text>
      </view>
      <view class="detail-item">
        <text class="detail-label">金额</text>
        <text class="detail-value text-danger font-bold">{{formatMoney(selectedRecord.amount)}}</text>
      </view>
      <view class="detail-item" wx:if="{{selectedRecord.ticketUrl}}">
        <text class="detail-label">票据</text>
        <image :src="selectedRecord.ticketUrl" class="ticket-image" mode="widthFix" />
      </view>
      <view class="detail-item" wx:if="{{selectedRecord.remark}}">
        <text class="detail-label">备注</text>
        <text class="detail-value">{{selectedRecord.remark}}</text>
      </view>
    </view>
    <view class="modal-footer">
      <button class="btn-secondary" bindtap="closeModal">关闭</button>
      <button class="btn-primary" bindtap="deleteRecord">删除</button>
    </view>
  </view>
</view>